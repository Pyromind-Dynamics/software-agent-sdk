---
title: 安装
description: 通过 PyPI 安装 pyromind-sdk，配置 API Key 与客户端参数。
---

## 通过 PyPI 安装

```bash
pip install pyromind-sdk
```

需要 Python >= 3.8。

## 初始化客户端

```python
from pyromind_sdk import PyroMindAPIClient
from pyromind_sdk.client.models import TrainingTaskCreateRequest

client = PyroMindAPIClient(api_key="your-api-key")

# 创建并运行一个 Studio 任务
task = client.studio.create(
    TrainingTaskCreateRequest(
        name="my-workflow",
        workflow={"nodes": [...]}
    )
)
print(f"Created task: {task.task_id}")
```

## 客户端参数

| 参数 | 必填 | 类型 | 默认值 | 说明 |
|------|------|------|--------|------|
| `api_key` | 是* | `str` | `PYROMIND_API_KEY` 环境变量 | API 认证的 Bearer Token |
| `cluster` | 否 | `str` | `PYROMIND_CLUSTER` 环境变量或 `"us-west-2"` | 目标集群（`X-Cluster` 请求头） |
| `timeout` | 否 | `int` | `30` | 请求超时时间（秒） |
| `max_retries` | 否 | `int` | `3` | 失败请求的最大重试次数 |

\* `api_key` 可作为参数传入，也可通过 `PYROMIND_API_KEY` 环境变量提供。

## 环境变量

| 变量 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `PYROMIND_API_KEY` | 是 | — | API Bearer Token |
| `PYROMIND_CLUSTER` | 否 | `us-west-2` | 目标集群标识 |
| `PYROMIND_STORAGE_ENDPOINT` | 否 | `https://storage.pyromind.ai` | 存储端点 URL |
| `PYROMIND_STORAGE_SECRET_KEY` | 否 | — | 存储 Secret Key |
| `PYROMIND_STORAGE_BUCKET` | 否 | — | 默认存储桶名称 |

## 下一步

- [创建开发环境](/zh/docs/sdk/jupyterlab)
- [执行训练工作流](/zh/docs/sdk/training-workflow)
- 遇到 API 错误时，参阅 [更多 SDK 信息 — 错误处理](/zh/docs/sdk/more#error-handling)
