# Packages baked into the agent terminal environment at build time.
# Add packages here to make them available to every agent terminal
# (runtime image system python, builder/local uv venvs) without any
# runtime pip installs. Consumed by: Dockerfile, Makefile make build.
wandb>=0.19
numpy>=1.26
pandas>=2.2