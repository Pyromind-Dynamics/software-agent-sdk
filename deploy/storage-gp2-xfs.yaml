---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
    name: gp2-xfs
provisioner: kubernetes.io/aws-ebs
parameters:
    type: gp2
    fsType: xfs
volumeBindingMode: WaitForFirstConsumer
mountOptions:
    - prjquota
